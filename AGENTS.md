# Repository Instructions

## プロジェクトの位置づけ

- このリポジトリは、小規模ワンオペ居酒屋向けのタブレット注文システムである。
- 現在実装されているアプリは `prototype/` 配下の Vite + React 19 フロントエンド試作である。
- 現在のデータ保存はブラウザの `localStorage`、同期は同一ブラウザの別タブ間に限られる。本番の複数実機通信、サーバー保存、認証、障害復旧が実装済みであるかのように扱わない。
- 外部の有料 API・有料 SaaS・継続が保証されない無料枠には依存せず、ローカルネットワーク内で自己完結する構成を優先する。

## 作業開始時の読書順と確認

1. 最初にこのルート `AGENTS.md` を最後まで読む。
2. `prototype/` を変更する場合は、次に `prototype/AGENTS.md` を最後まで読む。
3. `mvp-design-spec.md` を業務仕様の正本として扱う。
4. `prototype/design-qa.md` を現在のビジュアル受入記録として扱う。
5. `prototype/CONTEXT.md` で確認済みの現在地と未実装領域を確認する。ただし、Gitとコードの現況が異なる場合は現況を優先して同ファイルを更新する。
6. 変更前に作業フォルダ、origin、現在ブランチ、HEAD、ahead/behind、`git status --short`、対象差分を確認する。
7. 実装・変更を始める前に、推定所要時間と今回変更する範囲をユーザーへ伝える。

既存の未コミット変更はユーザーの作業として保持する。強制的な reset、checkout、上書き、無関係な整形や整理は行わない。変更は目的を満たす最小単位に限定し、依存関係の追加・更新や別目的のリファクタリングを混ぜない。

## 変更してはいけない業務ルール

- 客席画面には商品価格と合計金額を表示しない。
- 注文時点の正式品名と単価を履歴用スナップショットとして保持し、後のメニュー編集で書き換えない。
- 売り切れ変更は新規追加だけを止め、作成済み注文へ影響させない。
- キッチンでは未提供品を提供済み品より上段に置く。全品提供済みになった注文は新着から外して履歴へ移動する。
- 本番通信・保存を実装するときは、クライアント生成の注文 ID とサーバー側の冪等性で二重登録を防ぐ。画面上の疑似オフライン挙動を本番保証とみなさない。
- UIの基準は10インチ横向き、1280 x 800 CSS px、ブラウザズーム100%とする。

## 保護対象と安全性

次の Sites 関連ファイルは、その仕組み自体が作業対象でない限り変更しない。

- `prototype/.openai/hosting.json`
- `prototype/worker/index.js`
- `prototype/scripts/prepare-sites-build.mjs`
- `prototype/tests/sites-worker.test.mjs`

秘密情報、APIキー、認証トークン、実顧客情報、実注文情報をリポジトリ・ログ・スクリーンショット・テストデータへ追加しない。サンプルデータは架空であることが明確な内容だけを使う。

## リスク別の検証基準

### Low

文書、コメント、実行時挙動を変えない小規模な設定変更。

- 対象ファイルを読み直す。
- `git diff --check` と `git status --short` を実行する。
- 差分が依頼範囲だけであることを確認する。

### Medium

UI、スタイル、クライアント状態、業務ロジック、ビルド設定の変更。

- Lowの確認をすべて行う。
- `prototype/` でロックファイル固定の依存復元、`pnpm run build`、`pnpm run test:sites` を行う。
- 影響する操作フローを確認する。
- UI変更は1280 x 800 CSS px、100%ズームでスクリーンショットを取得し、文字、余白、タップ領域、はみ出し、業務ルールを確認する。

### High

実機間通信、認証・権限、サーバー・DB、注文状態やスキーマ、オフライン再送、冪等性、Worker・配信、バックアップ・復旧の変更。

- Mediumの確認をすべて行う。
- 互換性、移行、失敗時の復旧方法を先に定義する。
- 同時注文、通信断、再接続、再送、二重送信、価格変更中の注文、端末・サーバー再起動、バックアップ復元を実機相当で検証する。
- 未検証条件と残余リスクを明記し、ビルド成功だけで本番安全性が証明されたと判断しない。

環境制限や承認待ちで検証できない場合、依存・設定・既存コードを勝手に変更して回避しない。停止理由、実行できた代替検証、未検証範囲を報告する。

## セッション終了時の引き継ぎ

完了報告と `prototype/CONTEXT.md` の必要な更新には、次を簡潔に含める。

- 変更内容
- 変更していない範囲
- 実行した検証
- 未検証事項
- 残るリスク
- 次に行う作業を1つ
