# 小規模ワンオペ居酒屋タブレットオーダー

## 1. MVPの前提

- 客席タブレットは4台。各端末を固定のテーブル番号に割り当て、通常は客が席番号を選ばない。
- キッチン側端末は1台。キッチン画面・管理画面・提供済み履歴をここに集約する。
- 既存Wi‑Fiを使う。Bluetoothは使わない。
- 客席側では金額・合計金額を表示しない。金額はキッチン側の管理用データとして保持する。
- 注文完了時の効果音・アニメーション、QRスマホミラーリング、メニュー画像はコアフローの後に追加する。

## 2. 画面遷移

### 客席タブレット（4台）

1. 起動（キオスクモード）
2. 固定テーブル番号を確認（設定済みのため選択は不要）
3. カテゴリ一覧 → メニュー一覧
4. 名物カテゴリ／ポスター風メニュー → タップして直接注文候補に追加
5. 注文内容確認（品名・数量のみ。金額は非表示）
6. 「注文を送信」
7. 送信完了／送信待ち表示

メニュー画像は任意項目とし、初期状態ではテキスト中心で表示する。売り切れ商品は追加不可、価格変更は次回注文から反映する。

### キッチン端末

1. 「新着注文」
2. テーブルパネルを横スクロール（テーブル1、4、5などを横並びで確認）
3. 各パネル内は縦方向の品目リスト。未提供品を上段に固定し、提供済み品は下段へ移動または折りたたむ
4. 品目ごとにチェックを付ける（提供した時点でチェック）
5. 全品チェック済みになると自動的に「提供済み」へ移動
6. 左ナビの「提供済み（履歴）」で完了注文を確認

各テーブルパネルの下段に、管理用の合計金額を表示する。価格文字は、パネルが淡色・完了状態でもコントラストを落とさない。

### 管理画面（キッチン端末内）

- メニューの追加・編集・並び替え
- カテゴリの追加・並び替え
- 価格変更
- 売り切れ／販売中の切り替え
- メニュー画像の追加・差し替え（後から利用可能）
- 4台の端末とテーブル番号の固定割り当て
- 「端末・テーブル設定」から、各客席端末のテーブル番号を任意の番号へ変更できる
- 同じテーブル番号を複数の客席端末へ割り当てることはできず、保存時に重複を検出して警告する
- 割り当て変更は変更後に作成する新規注文から適用し、送信待ち注文と過去の注文履歴は注文作成時のテーブル番号を保持する

### 提供済み（履歴）

- 注文番号、テーブル、受付時刻、完了時刻、品目、数量、合計金額を表示
- MVPでは閲覧中心。訂正や会計処理は対象外

## 3. 注文状態と提供状態

### 注文状態

`new`（新着） → `active`（一部未提供） → `completed`（全品提供済み・履歴へ）

通信状態に応じて、送信前後に次の状態を持つ。

- `queued_offline`: 客席端末内に保存された送信待ち
- `sync_failed`: 再送が必要
- `syncedAt`: キッチン端末で受信・同期した時刻

### 品目状態

- `unserved`: 未提供。常に画面上で隠れないよう上段に表示
- `served`: 提供済み。チェックマークと提供時刻を表示し、下段へ移動

全品が `served` になった瞬間に注文を `completed` とし、通常の新着一覧から取り除いて履歴へ追加する。`調理開始`・`調理完了`の状態は持たない。

## 4. データ項目

### Device（端末）

| 項目 | 内容 |
|---|---|
| `deviceId` | 端末固有ID |
| `role` | `customer` / `kitchen` |
| `tableId` | 客席端末に割り当てるテーブル番号。キッチン端末は空欄 |
| `appVersion` | アプリバージョン |
| `lastSeenAt` | 最終接続時刻 |

### Table（テーブル）

| 項目 | 内容 |
|---|---|
| `tableId` | テーブル番号 |
| `label` | 画面表示名（例：テーブル1） |
| `deviceId` | 固定割り当て端末ID |
| `isActive` | 使用中／休止 |

### Category（カテゴリ）

`id`, `name`, `sortOrder`, `isVisible`, `updatedAt`

### MenuItem（メニュー）

`id`, `categoryId`, `name`, `description`, `price`, `isSoldOut`, `sortOrder`, `imageUri`（任意）, `updatedAt`

### Order（注文）

`id`, `tableId`, `createdAt`, `status`, `items`, `totalAmount`, `completedAt`, `syncedAt`

`totalAmount`はキッチン・管理用。客席画面には出さない。

### OrderItem（注文品目）

`menuItemId`, `nameSnapshot`, `unitPriceSnapshot`, `quantity`, `isServed`, `servedAt`

注文時点の品名・単価をスナップショット保存する。後からメニュー名や価格を変更しても、履歴の内容は変わらない。

### StaffCall（スタッフ呼び出し）

`id`, `tableId`, `type`, `createdAt`, `resolvedAt`

## 5. 業務ルール

- 売り切れ切り替えは新規追加だけを止め、既に入っている注文は変更しない。
- 価格変更は次回注文から適用する。既存注文の単価は `unitPriceSnapshot` を使う。
- 注文の再送には注文IDを使い、同じ注文が二重登録されないようにする。
- オフライン時は客席端末に送信待ちを表示してローカル保存する。再接続後に自動再送する。
- キッチン端末が受信できない間は、紙・口頭をバックアップ手段として案内する。過度な冗長化は行わない。
- 完了注文は通常画面で編集せず、履歴として閲覧する。

## 6. 通信・配置

```text
客席端末（4台・固定テーブル割り当て）
        │ 既存Wi‑Fi
        ▼
キッチン端末（1台）
  ├─ 新着注文／提供チェック
  ├─ 管理画面
  └─ 提供済み（履歴）
```

MVPではキッチン端末を注文データの基準端末とする。実装時に、キッチン端末がローカルサーバーも兼ねるか、別の小型ローカルサーバーを置くかを決定する。

## 7. MVP対象外

- 客席側の金額表示・会計・決済
- `調理開始`・`調理完了`の工程管理
- 注文完了アニメーション・効果音
- QRコードによるスマホミラーリング
- メニュー画像の必須化
- Bluetooth通信

## 8. 受け入れ確認

- 4台の客席端末から同時に注文して、キッチン画面に混ざらず届く
- 品数が8品以上でも、各テーブルパネル内で未提供品が縦に確認できる
- 一部だけ提供すると、その品目だけ下段へ移動し、未提供品は残る
- 全品チェックで自動的に履歴へ移動する
- 売り切れ・価格変更・並び順変更が管理画面から反映される
- 通信断・復旧後も注文が欠落せず、二重登録されない
- テーブル合計と履歴の単価が注文時点の値で一致する
